# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Editors / OS
.DS_Store
.idea/
.vscode/

# LaTeX build artefacts (sources and PDFs stay tracked)
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/*.toc

# Note: experiment logs (src/*.log, _*.log) are deliberately NOT ignored here —
# decide per file whether a run log belongs in the repo.
